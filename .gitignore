# Runtime / build
node_modules/
*.log
.DS_Store

# Beads / Dolt (runtime state, не в git)
.dolt/
.beads/*.db
.beads/*.sqlite
.beads-credential-key
# Task snapshot — source of truth в ветке beads-backup, в main не трекается
.beads/issues.jsonl

# Codex / external-review runtime
.codex/
.claude/skills/external-review/.runtime/

# Personal overrides
.claude/settings.local.json

# Install artifact: fail-closed guard кладёт сюда шаблон, если target уже имел AGENTS.md.
# Не коммитить — это временный merge-артефакт (INSTALL.md §B.3/§B.6).
AGENTS.md.overgate-template
